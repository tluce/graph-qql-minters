{
  "name": "qql-minters",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ qql-minters",
    "create-local": "graph create --node http://localhost:8020/ qql-minters",
    "remove-local": "graph remove --node http://localhost:8020/ qql-minters",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 qql-minters",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.34.0",
    "@graphprotocol/graph-ts": "0.28.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
